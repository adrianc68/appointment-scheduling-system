import { SlotDateRangePipe } from './slot-date-range.pipe';

describe('SlotDateRangePipe', () => {
  it('create an instance', () => {
    const pipe = new SlotDateRangePipe();
    expect(pipe).toBeTruthy();
  });
});
