import { DurationDatePipe } from './duration-date.pipe';

describe('DurationDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationDatePipe();
    expect(pipe).toBeTruthy();
  });
});
